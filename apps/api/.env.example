# Окружение API (Prisma + NestJS runtime).
# Скопируй в apps/api/.env. Значения должны совпадать с docker-compose (корневой .env).

DATABASE_URL="postgresql://ailin:ailin@localhost:5433/ailin_cinema?schema=public"

API_PORT=3000
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=12h
